---
let {node} = Astro.props;

if (node.markDef?.href?.startsWith("http") == false) {
  node.markDef.href = "/" + node.markDef.href;
  if (node.markDef?.href?.endsWith("/") === false) {
    node.markDef.href += "/";
  }
}
---

<a href={node.markDef.href}>{node.children[0].text}</a>
